{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "flow-bin": "^0.46.0",
    "gh-pages": "^1.0.0",
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "p2": "^0.7.1",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "homepage": "https://rileylark.github.io",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "flow": "flow"
  }
}
